<!doctype html>
<html lang="tr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="robots" content="noindex,nofollow">
    <title>Sistem Girişi | Karacabey Gross Market</title>
    <style nonce="{{ request()->attributes->get('csp_nonce') }}">
        :root {
            --v1: hsl(0, 0%, 98%);
            --v2: hsl(0, 0%, 100%);
            --v3: hsl(222.2, 84%, 4.9%);
            --v4: hsl(215.4, 16.3%, 46.9%);
            --v5: hsl(25, 95%, 53%); 
            --v6: hsl(25, 95%, 45%); 
            --v7: hsl(214.3, 31.8%, 91.4%);
            --v8: hsl(0, 84.2%, 96%);
            --v9: hsl(0, 84.2%, 60.2%);
        }
        
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body { font-family: system-ui, -apple-system, sans-serif; background: var(--v1); color: var(--v3); min-height: 100vh; display: flex; flex-direction: column; }
        
        ._wX1 { display: flex; min-height: 100vh; flex-direction: column; width: 100%; }
        
        ._wL2 { 
            position: relative; display: flex; flex-direction: column; justify-content: center; 
            padding: 3rem; color: white; overflow: hidden;
            background: linear-gradient(-45deg, #ea580c, #9a3412, #0f172a, #1e293b);
            background-size: 400% 400%; animation: _bgShift 15s ease infinite;
        }
        @keyframes _bgShift { 0% { background-position: 0% 50%; } 50% { background-position: 100% 50%; } 100% { background-position: 0% 50%; } }

        ._wL2 h1 { font-size: 2.2rem; font-weight: 800; margin-bottom: 1.5rem; line-height: 1.2; z-index: 10; letter-spacing: -0.02em; }
        ._wL2 h3 { font-size: 1rem; color: #fba11b; margin-bottom: 0.5rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.05em; z-index: 10; }
        ._wL2 p { font-size: 0.95rem; font-weight: 300; opacity: 0.90; margin-bottom: 1.5rem; z-index: 10; line-height: 1.6; }
        ._wL2 ._secBlock { margin-bottom: 1.5rem; z-index: 10; background: rgba(0,0,0,0.15); padding: 1rem; border-radius: 0.5rem; border-left: 3px solid #ea580c; }
        
        ._jsTyping { font-family: monospace; font-size: 0.85rem; background: rgba(0,0,0,0.5); padding: 1rem; border-radius: 0.5rem; border-left: 4px solid var(--v9); min-height: 80px; z-index: 10; line-height: 1.5; color: #4ade80;}
        ._cursor { display: inline-block; width: 8px; height: 15px; background: white; animation: _blink 1s step-end infinite; vertical-align: middle; margin-left: 4px; }
        @keyframes _blink { 50% { opacity: 0; } }

        ._wR3 { flex: 1; display: flex; align-items: center; justify-content: center; padding: 2rem; background: var(--v1); }
        ._bY2 { width: 100%; max-width: 26rem; background: var(--v2); border: 1px solid var(--v7); border-radius: 1rem; box-shadow: 0 10px 15px -3px rgba(0,0,0,0.1); overflow: hidden; }
        ._cZ3 { height: 4px; width: 100%; background: var(--v5); }
        ._dW4 { padding: 3rem 2.5rem; }
        
        ._logoContainer { display: flex; justify-content: center; margin-bottom: 2rem; }
        ._logo { max-height: 60px; object-fit: contain; }

        ._iL9 { display: flex; align-items: center; gap: 0.75rem; border-radius: 0.5rem; border: 1px solid var(--v9); background: var(--v8); padding: 1rem; color: var(--v9); font-size: 0.875rem; margin-bottom: 1.5rem; font-weight: 500; }
        
        ._jR0 { display: flex; flex-direction: column; gap: 1.5rem; }
        ._kS1 { display: flex; flex-direction: column; gap: 0.5rem; position: relative; }
        ._lT2 { font-size: 0.875rem; font-weight: 600; color: #334155; }
        ._inpWrapper { position: relative; display: flex; align-items: center; }
        ._inpIcon { position: absolute; left: 1rem; color: #94a3b8; pointer-events: none; }
        ._mU3 { display: flex; height: 3rem; width: 100%; border-radius: 0.5rem; border: 1px solid var(--v7); background: #f8fafc; padding: 0.25rem 1rem 0.25rem 2.75rem; font-size: 0.9rem; transition: all 0.2s; outline: none; font-family: inherit; color: var(--v3); }
        ._mU3:focus { background: #fff; border-color: var(--v5); box-shadow: 0 0 0 4px rgba(249, 115, 22, 0.1); }
        ._mU3::placeholder { color: #94a3b8; }
        
        ._nV4 { margin-top: 2.5rem; }
        ._oW5 { display: inline-flex; height: 3rem; width: 100%; align-items: center; justify-content: center; border-radius: 0.5rem; background: var(--v5); color: white; font-size: 0.95rem; font-weight: 600; border: none; cursor: pointer; transition: all 0.2s; font-family: inherit; box-shadow: 0 4px 6px -1px rgba(249, 115, 22, 0.3); }
        ._oW5:hover { background: var(--v6); transform: translateY(-1px); box-shadow: 0 6px 8px -1px rgba(249, 115, 22, 0.4); }

        @media (min-width: 992px) {
            ._wX1 { flex-direction: row; }
            ._wL2 { flex: 1.1; padding: 4rem; }
            ._wR3 { flex: 1; }
        }
    </style>
</head>
<body>

    <div class="_wX1">
        <div class="_wL2">
            <h1>KGM Sistem Altyapısı<br>& Güvenlik Protokolleri</h1>
            
            <div class="_secBlock">
                <h3>Kriptografik İletişim ve WAF İzlemesi</h3>
                <p>Bu gateway üzerinden yapılan tüm veri akışı <strong>TLS 1.3</strong> protokolü ve <strong>AES-256-GCM</strong> şifreleme standartlarıyla uçtan uca korunmaktadır. Web Application Firewall (WAF) modülümüz; SQLi, XSS ve Zero-Day payload analizlerini gerçek zamanlı yapar ve şüpheli paketleri anında <em>drop</em> eder.</p>
            </div>

            <div class="_secBlock">
                <h3>Adli Bilişim Loglaması (Immutable Logs)</h3>
                <p>Brute-Force (Kaba Kuvvet) saldırılarına karşı aktif <em>Rate-Limiting</em> devrededir. Başarılı veya başarısız her HTTP/S isteği; <strong>X-Forwarded-For IP adresi, ASN bilgisi, cihaz/tarayıcı fingerprint'i (parmak izi)</strong> ile birlikte değiştirilemez log sunucularına yazılmaktadır.</p>
            </div>

            <div class="_secBlock">
                <h3 style="color: #ef4444;">Yasal Sorumluluk ve Yaptırım</h3>
                <p style="margin-bottom: 0;">Sisteme yönelik izinsiz erişim denemeleri ve sızma girişimleri, Türk Ceza Kanunu (TCK) Madde 243 ve 244 kapsamında otomatik olarak adli mercilere raporlanmaktadır.</p>
            </div>
            
            <div class="_jsTyping">
                <span id="typewriterText"></span><span class="_cursor"></span>
            </div>
        </div>

        <div class="_wR3">
            <div class="_bY2">
                <div class="_cZ3"></div>
                
                <form action="{{ route('admin.login.store') }}" method="post" class="_dW4">
                    @csrf
                    
                    <div class="_logoContainer">
                        <img src="{{ asset('assets/kgm-logo-4k.png') }}" alt="Karacabey Gross Market" class="_logo">
                    </div>

                    @error('email')
                        <div class="_iL9">
                            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <circle cx="12" cy="12" r="10"/><line x1="12" x2="12" y1="8" y2="12"/><line x1="12" x2="12.01" y1="16" y2="16"/>
                            </svg>
                            <span>{{ $message }}</span>
                        </div>
                    @enderror

                    <div class="_jR0">
                        <div class="_kS1">
                            <label for="email" class="_lT2">Sistem E-posta Adresi</label>
                            <div class="_inpWrapper">
                                <svg class="_inpIcon" xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path><polyline points="22,6 12,13 2,6"></polyline></svg>
                                <input id="email" name="email" type="email" autocomplete="email" required autofocus class="_mU3" placeholder="ornek@karacabeygross.com">
                            </div>
                        </div>

                        <div class="_kS1">
                            <label for="password" class="_lT2">Yetkili Şifresi</label>
                            <div class="_inpWrapper">
                                <svg class="_inpIcon" xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="11" width="18" height="11" rx="2" ry="2"></rect><path d="M7 11V7a5 5 0 0 1 10 0v4"></path></svg>
                                <input id="password" name="password" type="password" autocomplete="current-password" required class="_mU3" placeholder="••••••••••••">
                            </div>
                        </div>
                    </div>

                    <div class="_nV4">
                        <button type="submit" class="_oW5">Güvenli Giriş Yap</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script nonce="{{ request()->attributes->get('csp_nonce') }}">
        document.addEventListener('DOMContentLoaded', () => {
            const textArray = [
                "> init_security_protocol.sh ... OK",
                "> WAF Kalkanı: AKTİF",
                "> TLS 1.3 Handshake: BAŞARILI",
                "> Cihaz Fingerprint taraması tamamlandı.",
                "> UYARI: İzinsiz denemeler raporlanacaktır."
            ];
            const typingDelay = 35;
            const erasingDelay = 15;
            const newTextDelay = 2000;
            let textArrayIndex = 0;
            let charIndex = 0;
            const typedTextSpan = document.getElementById("typewriterText");

            function type() {
                if (charIndex < textArray[textArrayIndex].length) {
                    typedTextSpan.textContent += textArray[textArrayIndex].charAt(charIndex);
                    charIndex++;
                    setTimeout(type, typingDelay);
                } else {
                    setTimeout(erase, newTextDelay);
                }
            }

            function erase() {
                if (charIndex > 0) {
                    typedTextSpan.textContent = textArray[textArrayIndex].substring(0, charIndex - 1);
                    charIndex--;
                    setTimeout(erase, erasingDelay);
                } else {
                    textArrayIndex++;
                    if (textArrayIndex >= textArray.length) textArrayIndex = 0;
                    setTimeout(type, typingDelay + 500);
                }
            }
            setTimeout(type, 1000);
        });
    </script>
</body>
</html>